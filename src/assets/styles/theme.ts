import { colors } from './colors';
import { fontSizes, fontFaces } from './fonts';
import { images } from './images';

export const theme = {
  ...colors,
  ...fontSizes,
  ...images,
  ...fontFaces,
};
